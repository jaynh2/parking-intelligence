[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # line length handled by formatting, not a hard lint failure

[tool.pytest.ini_options]
testpaths = ["tests"]
